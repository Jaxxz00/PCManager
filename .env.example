# ==============================================
# PCManager - Variabili d'Ambiente
# ==============================================
# Copia questo file in .env e configura i valori

# ----------------------------------------------
# Database Configuration
# ----------------------------------------------
# MySQL/MariaDB connection string
# Formato: mysql://username:password@host:port/database
# Se non specificato, usa JSON storage in-memory (solo per sviluppo)
DATABASE_URL=mysql://pcmanager:password@localhost:3306/pcmanager

# ----------------------------------------------
# Server Configuration
# ----------------------------------------------
# Porta su cui gira l'applicazione (default: 5000)
PORT=5000

# Ambiente di esecuzione (development | production)
NODE_ENV=production

# URL pubblico dell'applicazione (per link inviti)
APP_URL=https://tuodominio.com

# ----------------------------------------------
# Security Configuration
# ----------------------------------------------
# Chiave segreta per le sessioni - GENERA UNA STRINGA RANDOM SICURA!
# Comando per generare: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET_KEY_IN_PRODUCTION

# ----------------------------------------------
# Email Configuration (OPZIONALE)
# ----------------------------------------------
# SendGrid API key per invio email automatico
# Se non configurato, il sistema genera link di invito da condividere manualmente
# SENDGRID_API_KEY=your_sendgrid_api_key_here
# FROM_EMAIL=noreply@yourdomain.com

# ----------------------------------------------
# Google Cloud Storage (OPZIONALE)
# ----------------------------------------------
# Per upload documenti su Google Cloud Storage
# Se non configurato, i file vengono salvati localmente
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_CLOUD_KEY_FILE=/path/to/service-account.json
# GOOGLE_CLOUD_BUCKET_NAME=your-bucket-name

# ----------------------------------------------
# Rate Limiting (opzionale, già configurato)
# ----------------------------------------------
# Configurazione rate limiting API (richieste per finestra temporale)
# API_RATE_LIMIT_WINDOW_MS=900000
# API_RATE_LIMIT_MAX=100
# LOGIN_RATE_LIMIT_MAX=5
# QR_RATE_LIMIT_MAX=20
